after cloning run npm install in terminal 


packages intalled
express 
prisma 
@prisma/client 
bcrypt 
jsonwebtoken 
nodemailer 
dotenv 
cors 
uuid



// this automatically formats your prisma code
npx prisma format


//display database on local host
npx prisma studio


//Testing endpoints
1. Register a new user:
POST localhost:5000/api/auth/register 
  -H "Content-Type: application/json" \
  -{
    "email": "john@example.com",
    "password": "password123",
    "firstName": "John",
    "lastName": "Doe"
  }

  2. Verify email (copy token from email or database)
    GET localhost:5000/api/auth/verify-email?token=TOKEN_FROM_EMAIL

3. Login
POST localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  {
    "email": "john@example.com",
    "password": "password123"
  }

4. Get current user (protected route):
GET http://localhost:5000/api/auth/me \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"